# Simple LOGIN (Express) + CRUD + FRONT + Bcrypt + dotenv
    * FRONT SIMPLE -> Login + Create new user + forward password.
    * TABLE = 
        userId (unique, pk, nn)
        name (nn)
        lastname (nn)
        email (unique, nn)
        username (unique, nn)
        pass (nn) + Encription
    * Bcrypt Password
    * dotenv



